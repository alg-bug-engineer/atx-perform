# Vite 前端环境变量。复制为 frontend/.env.local 后按需修改。

# 空 = 同域 /api/v1（开发时 Vite 代理到 http://127.0.0.1:8000）
VITE_API_BASE=

# 1 = 幕 JSON 走后端 /api/v1/scenes；0 = 仍从 @data 打包（默认可单开前端）
VITE_SCENE_API=0

# 0 = Live Agent（需后端 Agent 模块）；非 0 = Mock fixture
VITE_MOCK=1

VITE_TTS_ENABLED=false
VITE_DATA_MODE=demo
