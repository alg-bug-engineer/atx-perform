---
description: 分幕 JS 与 data JSON 约定
globs: frontend/**/*.{js,vue,ts},data/**/*.json
alwaysApply: false
---

# 分幕实现约定

- 一幕一个目录：`frontend/src/scenes/scene{N}-*/`（`index.js` + Vue），禁止揉进单文件大杂烩。
- 独立调试：`?scene=0..5`（别名 `plan`/`effect`/`skill`），见 `frontend/README.md`。
- 数据只通过 `services/loadSceneData.js` 读 `@data/1-*.json`；不要硬编码第二份业务 ID。
- 幕间禁止强依赖上一幕运行时状态；分工开发 3/4/5 时可直接打开对应 `?scene=`。
- 讲解对齐用 `actions[]`（`when` + `do` + `bind`）；先做动作，语音后配。
- 图层/配色用 `styles/theme.css`（baseline 青/夜景），不要引入 agent-loop 紫系主题。
- 缺失字段走 JSON 内 `db_supported` / `gaps`，UI 可降级但要可追踪。
---
